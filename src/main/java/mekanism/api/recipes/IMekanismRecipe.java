package mekanism.api.recipes;

public interface IMekanismRecipe {
    //TODO: 1.14 Ingredient extends java's Predicate instead of google's, so it will make sense to change places that store Ingredient to instead use Predicate<ItemStack>
}