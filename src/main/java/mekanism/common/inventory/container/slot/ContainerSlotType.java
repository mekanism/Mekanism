package mekanism.common.inventory.container.slot;

//TODO: Should this be in the API package?
public enum ContainerSlotType {
    IGNORED,
    NORMAL,
    POWER,
    INPUT,
    EXTRA,
    OUTPUT,
    VALIDITY;
}