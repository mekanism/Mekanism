package mekanism.common.tile.component.config.slot;

//TODO: Should we store some pointer or something to the thing we are storing the heat in?
public class HeatSlotInfo extends BaseSlotInfo {

    protected HeatSlotInfo(boolean canInput, boolean canOutput) {
        super(canInput, canOutput);
    }
}