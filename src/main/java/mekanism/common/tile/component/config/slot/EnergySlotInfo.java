package mekanism.common.tile.component.config.slot;

//TODO: Should we store some pointer or something to the thing we are charging?
public class EnergySlotInfo extends BaseSlotInfo {

    public EnergySlotInfo(boolean canInput, boolean canOutput) {
        super(canInput, canOutput);
    }
}